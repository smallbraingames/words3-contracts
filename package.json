{
    "name": "words3-contracts",
    "workspaces": [
        "packages/*"
    ],
    "private": true,
    "scripts": {
        "lint": "forge fmt packages/game && forge fmt packages/rng && bun run solhint",
        "solhint": "solhint --config ./.solhint.json '**/src/**/*.sol' --fix"
    },
    "engines": {}
}